mod api;

pub use self::api::Query;
pub use authorization_core::effect::Effect;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
